# WildFly Ecosystem AI Context Hub

> This is the global index for the WildFly ecosystem, covering core server runtime, full application server distribution, provisioning tooling, and cross-cutting architectural guidance. Agents and LLMs should use this file as the primary entry point for navigating WildFly's multi-repository codebase. Each satellite repository maintains its own `llms.txt` with component-level detail; this hub links them together and provides ecosystem-wide context that no single repository owns.

## Core & Runtime Repositories

- [WildFly Core](https://raw.githubusercontent.com/kabir/wildfly-core/ai-index/llms.txt): Core server management model, controller, CLI, and subsystem parsing. Defines the kernel runtime, management resource tree, operation handlers, deployment scanner SPI, and the Remoting-based management protocol. All subsystems in wildfly-core are boot-critical and available in both standalone and domain mode.
- [WildFly Full](https://raw.githubusercontent.com/kabir/wildfly/ai-index/docs/src/main/asciidoc/llms.txt): Full application server, Jakarta EE subsystem guides, security, HA, and deployment. Extends wildfly-core with Jakarta EE specifications (Servlet, EJB, JPA, JMS, CDI, JAX-RS, MicroProfile), clustering, Elytron security integration, and the complete distribution packaging.

## Ecosystem Tooling

- [WildFly Maven Plugin](https://raw.githubusercontent.com/kabir/wildfly-maven-plugin/ai-index/llms.txt): Developer-facing Maven build tooling for provisioning, packaging, deploying, and running WildFly servers. Provides 15 Maven goals including Galleon-based server provisioning, Bootable JAR packaging, container image building, dev-mode with hot-reload, and CLI script execution. Integrates WildFly Glow for automatic discovery of required feature packs and layers via `<discover-provisioning-info/>`.
- [WildFly Glow](https://raw.githubusercontent.com/kabir/wildfly-glow/ai-index/llms.txt): Provisioning analysis engine that scans application deployments (WAR, JAR, EAR) to determine the minimal set of Galleon feature packs and layers required. Provides a core scanning library (used by the WildFly Maven Plugin), a standalone CLI, an Arquillian Maven plugin for test provisioning, and OpenShift deployment support with automatic service deployers for PostgreSQL, MySQL, MariaDB, AMQ Broker, and Keycloak. Also supports JBang integration for running single Java files in WildFly.

- [WildFly Galleon Feature Packs](https://raw.githubusercontent.com/kabir/wildfly-galleon-feature-packs/ai-index/llms.txt): Central registry of Galleon feature packs for each WildFly server version. Contains per-version provisioning XML files listing compatible feature packs for bare-metal and cloud contexts, plus JSON catalogs for tooling. WildFly Glow consumes this registry from the `release` branch to auto-discover feature packs during server provisioning.

## Galleon Feature Pack Extensions

- [WildFly Cloud Galleon Pack](https://raw.githubusercontent.com/kabir/wildfly-cloud-galleon-pack/ai-index/llms.txt): Cloud-optimized Galleon feature pack that adjusts WildFly configuration for OpenShift/Kubernetes. Configures TCP-based JGroups clustering, console logging, health probes, and environment-variable-driven datasource/security/messaging configuration. Includes launch scripts and a bootable JAR cloud configurator runtime extension.
- [WildFly Datasources Galleon Pack](https://raw.githubusercontent.com/kabir/wildfly-datasources-galleon-pack/ai-index/llms.txt): Galleon layers for JDBC database drivers and datasource configurations. Supports PostgreSQL, MySQL, MariaDB, Oracle, DB2, MS SQL Server, H2, and AWS JDBC Wrapper with a three-layer pattern per database (driver, datasource, default-datasource). Environment-variable-configurable for cloud deployments.
- [WildFly gRPC Feature Pack](https://raw.githubusercontent.com/kabir/wildfly-grpc-feature-pack/ai-index/llms.txt): Adds gRPC subsystem support to WildFly via a `grpc` Galleon layer. Auto-discovers `BindableService` implementations at deployment time and registers them with a Netty-based gRPC server. Supports TLS, server interceptors, and per-deployment configuration.
- [WildFly MyFaces Feature Pack](https://raw.githubusercontent.com/kabir/wildfly-myfaces-feature-pack/ai-index/llms.txt): Provides Apache MyFaces 4.x as an alternative Jakarta Faces implementation via a `myfaces` Galleon layer. Includes a custom injection bridge integrating MyFaces with WildFly's CDI and annotation scanning infrastructure.
- [WildFly MicroProfile GraphQL Feature Pack](https://raw.githubusercontent.com/kabir/wildfly-graphql-feature-pack/ai-index/llms.txt): Adds MicroProfile GraphQL support via a `microprofile-graphql` Galleon layer backed by SmallRye GraphQL. Enables GraphQL API endpoints, typesafe GraphQL clients (including Vert.x-based), and the GraphiQL developer UI.
- [WildFly Vault Feature Pack](https://raw.githubusercontent.com/kabir/wildfly-vault-feature-pack/ai-index/llms.txt): Integrates HashiCorp Vault with WildFly for secure credential management via a `hashicorp-vault` Galleon layer. Provides Elytron credential store integration, `${HC_VAULT::...}` expression resolution, and HTTPS/mTLS Vault connectivity.

## Ecosystem Tooling (Placeholders/Future)

- WildFly Operator: Kubernetes operator for managing WildFly application deployments on OpenShift and vanilla Kubernetes. Handles StatefulSet orchestration, session draining, and transaction recovery. Repository index forthcoming.

## Global Architecture & Contracts

- [Ecosystem Topology](./architecture/ecosystem-topology.md): Component relationships and execution flow across repositories. Documents the hierarchy from core runtime through full distribution to provisioning and deployment tooling.
- [Repository Boundaries](./architecture/repository-boundaries.md): SPI boundaries and dependency rules between Core, WildFly, and provisioning tools. Defines what belongs where and the contract versioning discipline that keeps repositories independently releasable.
- [ADR 0001: Hub-and-Spoke AI Context](./adrs/0001-hub-and-spoke-ai-context.md): Architectural Decision Record for the multi-repo AI context model. Explains why a federated index approach was chosen over monolithic context dumps or per-query repository crawling.
